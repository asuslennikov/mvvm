<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/raw_color_blue_light</item>
        <item name="colorPrimaryDark">@color/raw_color_blue_dark</item>
        <item name="colorAccent">@color/raw_color_yellow_dark</item>
        <item name="windowActionBarOverlay">false</item>
        <item name="windowActionBar">false</item>
    </style>

</resources>
